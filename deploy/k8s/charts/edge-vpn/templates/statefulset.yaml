{{- /* deploy/k8s/charts/edge-vpn/templates/statefulset.yaml */ -}}
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: {{ include "edge-vpn.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "edge-vpn.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  serviceName: {{ include "edge-vpn.fullname" . }}-headless
  podManagementPolicy: Parallel
  selector:
    matchLabels:
      {{- include "edge-vpn.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "edge-vpn.selectorLabels" . | nindent 8 }}
    spec:
      # Prevent the Edge VPN DNS sidecar from being used for its own DNS lookups.
      # Without this, DNS queries loop back to the sidecar before it's ready, causing
      # startup failures. kube-dns resolves internal service names; 8.8.8.8 handles external.
      dnsPolicy: None
      dnsConfig:
        nameservers:
          - {{ .Values.dns.kubeDnsIp }}
          - 8.8.8.8
        searches:
          - {{ .Release.Namespace }}.svc.cluster.local
          - svc.cluster.local
          - cluster.local

      volumes:
        # Shared between Edge VPN server (writes netmaker.hosts) and Edge VPN DNS (reads netmaker.hosts)
        - name: dnsconfig
          emptyDir: {}
        # Edge VPN DNS Corefile config
        - name: edge-vpn-dns-config
          configMap:
            name: {{ include "edge-vpn.fullname" . }}-corefile
        # EMQX auth/ACL hocon — mounted into EMQX data dir so it is loaded at startup
        - name: emqx-hocon
          configMap:
            name: {{ include "edge-vpn.fullname" . }}-emqx-hocon

      containers:
        # --- Edge VPN Server ---
        - name: edge-vpn-server
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - name: api
              containerPort: 8081
              protocol: TCP
          envFrom:
            - configMapRef:
                name: {{ include "edge-vpn.fullname" . }}-env
            - secretRef:
                name: {{ include "edge-vpn.secretName" . }}
          volumeMounts:
            - name: dnsconfig
              mountPath: /root/config/dnsconfig
            - name: edge-vpn-server-data
              mountPath: /root/data
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
          readinessProbe:
            httpGet:
              path: /api/server/status
              port: api
            initialDelaySeconds: 10
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 6
          livenessProbe:
            httpGet:
              path: /api/server/status
              port: api
            initialDelaySeconds: 30
            periodSeconds: 30
            timeoutSeconds: 5
            failureThreshold: 3

        # --- Edge VPN DNS sidecar ---
        # Reads /dnsconfig/netmaker.hosts written by the Edge VPN server above.
        # Exposes .nm.internal resolution to the cluster via the dns-svc Service.
        - name: edge-vpn-dns
          image: "{{ .Values.coredns.image }}:{{ .Values.coredns.tag }}"
          imagePullPolicy: {{ if eq .Values.coredns.tag "latest" }}Always{{ else }}IfNotPresent{{ end }}
          args:
            - "-conf"
            - "/etc/coredns/Corefile"
          # NET_BIND_SERVICE is required to bind port 53 (privileged port) as non-root
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              add:
                - NET_BIND_SERVICE
              drop:
                - ALL
          ports:
            - name: dns-udp
              containerPort: 53
              protocol: UDP
            - name: dns-tcp
              containerPort: 53
              protocol: TCP
          volumeMounts:
            - name: dnsconfig
              mountPath: /dnsconfig
            - name: edge-vpn-dns-config
              mountPath: /etc/coredns
          resources:
            {{- toYaml .Values.corednsResources | nindent 12 }}
          readinessProbe:
            httpGet:
              path: /ready
              port: 8181
            initialDelaySeconds: 5
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 10
            periodSeconds: 30
            timeoutSeconds: 5
            failureThreshold: 3

  volumeClaimTemplates:
    - metadata:
        name: edge-vpn-data
      spec:
        accessModes:
          - ReadWriteOnce
        storageClassName: {{ .Values.persistence.storageClassName }}
        resources:
          requests:
            storage: {{ .Values.persistence.size }}
