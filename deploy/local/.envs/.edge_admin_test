# deploy/local/.envs/.edge_admin_test
MIX_ENV=test
EDGE_ADMIN_MODE=test
API_PORT=44000

# Authentication configuration (disabled for tests)
AUTH_ENABLED=false

# Phoenix/Session (needed for controller tests)
SECRET_KEY_BASE=G0ieeRljoXGzSDPRrYc2q4ADyNHCwxNOkw7YpPNMa+JgP9iGgJKT4K96Bw/Mf/pd

# Database configuration (tests use real DB with Ecto sandbox)
DB_HOST=edge_admin_db
DB_PORT=5432
DB_NAME=edge_admin_test
DB_USER=edge_admin_user
DB_PASSWORD=edge_admin_password
DB_POOL_SIZE=2
DB_SSL=false

# Admin configuration (required by runtime.exs)
ADMIN_MAX_CAPACITY=100
ADMIN_CLUSTER_NAME=test
ADMIN_CLUSTER_SUBNET=100.71.0.0/16

# Netmaker configuration (required by runtime.exs)
NETMAKER_API_URL=http://edge_vpn:8081
NETMAKER_MASTER_KEY=test_master_key
NETMAKER_SUPERADMIN_USERNAME=admin
NETMAKER_SUPERADMIN_PASSWORD=password
NETMAKER_DEFAULT_DOMAIN=nm.internal

# Metrics configuration
METRICS_BASE_URL=http://edge_metrics_storage:8428

# Cluster configuration
CLUSTER_SUBNET_PREFIX=24
CLUSTER_AUTO_GENERATED_RANGES=100.64.0.0/10

# Proxy configuration
HTTP_PROXY_PORT=43128
SOCKS5_PROXY_PORT=41080

# Erlang distribution
ERLANG_COOKIE=test_cookie
ADMIN_DISCOVERY_PORT=44000

# Optional: Enable detailed test output
# TRACE_TESTS=true

# Netmaker (for netclient userspace mode)
WG_QUICK_USERSPACE_IMPLEMENTATION=wireguard-go